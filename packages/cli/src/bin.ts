#!/usr/bin/env node
/**
 * @astack/cli — Command-line interface.
 *
 * Commands:
 *   astack init               — register current project
 *   astack subscribe <skill>  — subscribe to meta-skills
 *   astack sync               — pull latest from upstream
 *   astack push <skill>       — push local changes to upstream
 *   astack status             — show sync status
 *   astack diff <skill>       — local vs upstream diff
 *   astack resolve <skill>    — resolve conflicts
 *   astack link <cmd>         — manage tool symlinks
 *   astack server <cmd>       — manage daemon
 *
 * See docs/asset/design.md § CLI 核心工作流 for spec.
 */

console.log("astack: not implemented yet");
process.exit(0);
